<p style="color: green"><%= notice %></p>

<h1>Users</h1>

<div id="users">
  <% @users.each do |user| %>
    <%=  user.name %>
    <p>
      <%= link_to "Show this user", user %>
      <%= link_to "Show shopping-cart", user.shopping_cart %>
    </p>
  <% end %>
</div>

<%= link_to "New user", new_user_path %>
