<%= form_with(model: offer) do |form| %>
  <% if offer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(offer.errors.count, "error") %> prohibited this offer from being saved:</h2>

      <ul>
        <% offer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :shopping_cart_id, style: "display: block" %>
    <%= form.text_field :shopping_cart_id %>
  </div>

  <div>
    <%= form.label :brick_id, style: "display: block" %>
    <%= form.text_field :brick_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
